export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
export default function TaskItem({ task, onRemove }) {
  return (
    <li className="flex justify-between items-center bg-gray-100 rounded-md px-4 py-2 mb-2">
      <span>{task}</span>
      <button
        onClick={onRemove}
        className="bg-red-500 hover:bg-red-600 text-white rounded px-2 py-1"
        aria-label="Remove task"
      >
        ✕
      </button>
    </li>
  );
}
